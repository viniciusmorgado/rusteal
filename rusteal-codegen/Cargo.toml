[package]
name = "rusteal-codegen"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Code generator: UHT JSON to Rust bindings and C++ wrappers for rusteal"
build = "build.rs"
include = ["src/**", "manual/**", "build.rs", "Cargo.toml"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
rusteal-ue-flags = { workspace = true }
