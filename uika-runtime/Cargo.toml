[package]
name = "uika-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Safe Rust runtime for uika (object refs, lifecycle, dynamic calls)"

[dependencies]
uika-ffi = { workspace = true }
glam = { workspace = true }
inventory = { workspace = true }

[build-dependencies]
syn = { version = "2", features = ["full"] }
