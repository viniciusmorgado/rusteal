[package]
name = "example-game"
version = "0.1.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
uika = { path = "../uika", features = ["engine"] }
glam = { workspace = true }
