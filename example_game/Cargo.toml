[package]
name = "example-game"
version = "0.1.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rusteal-runtime = { path = "../rusteal-runtime", features = ["engine"] }
glam = { workspace = true }
