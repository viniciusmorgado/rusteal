[package]
name = "rusteal-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "FFI types and API table definitions for rusteal"

[dependencies]
rusteal-ue-flags = { workspace = true }
